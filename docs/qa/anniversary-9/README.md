# 九周年首屏验收

验收日期：2026-09-22。主智能体负责构图、SVG、动画和全部截图复核；工程子任务使用 GPT 5.6 Terra Max。

## 交付结果

- 细线数字 9 由圆环与笔直斜向尾部组成，四幕共用同一 SVG；右下角文字为 `th Anniversary`。
- 花卉动漫 → 自然科学与 AI → 人文 → 现代，完整有效播放时长 22.6 秒。最后停留在现代版，只能通过重播按钮重新开始。
- 花枝、花簇、分镜、网格、轨道、网络节点与纸片独立进行位移、旋转和透明度过渡，首屏背景随场景轻微变化，无分页点。
- 暂停、离屏、页面隐藏时停止推进；减少动态效果时直接显示静态现代版。
- 原版 Hero 与改版前归档逐字对比一致，仅组件导出名称改为 `ClassicHero`；原版 CSS 字节一致。

## 浏览器验证

Chrome 与 WebKit 均通过以下检查，页面运行错误为零：

| 检查                                        | 结果                                                             |
| ------------------------------------------- | ---------------------------------------------------------------- |
| 暂停后推进测试时钟                          | 时间轴与样式保持不变                                             |
| 键盘 Space 操作暂停按钮                     | 正常切换                                                         |
| 滚动到首屏以外，再滚回                      | 停止并从原进度继续                                               |
| 模拟 `visibilitychange` / `visibilityState` | 隐藏时停止，显示后继续；此项是状态模拟，不是操作系统切换标签测试 |
| 过渡中读取部件变换矩阵                      | 不同部件有不同的有效变换                                         |
| 最后一幕后再推进 60 秒                      | 保持现代版，运行状态为 false                                     |
| 手动重播                                    | 回到第一幕                                                       |
| 减少动态效果                                | 静态现代版，不显示动画控制                                       |
| 1440、1050、900、768、601、390、360px       | 无页面横向溢出；周年文字不越界、不与按钮重叠                     |

主要视觉截图覆盖 1440、768、390、360px。逐幕桌面截图：
[花卉动漫](anime-1440.png)、[科学与 AI](science-1440.png)、[人文](humanities-1440.png)、[现代](modern-1440.png)。
另有 [WebKit 过渡截图](webkit-component-transition.png)、[手机现代版](webkit-modern-390.png) 和 [恢复原版截图](classic-restored-1440.png)。

自动记录见 [基础动画检查](results.json)、[跨浏览器检查](cross-browser-results.json)、[版本切换检查](edition-switch-results.json)。时间轴使用 Playwright 测试时钟推进，截图与 DOM/CSS 检查均在实际浏览器执行。

## 代码与回退

- `pnpm test`：7 个文件、36 项测试通过。
- `pnpm typecheck`、`pnpm lint`、`pnpm build`：通过。构建仍提示现有媒体模块的两项动态文件系统追踪警告，本轮未修改媒体模块。
- 实际执行 `pnpm site:edition classic` 后原版立即生效；再执行 `pnpm site:edition anniversary-9` 后九周年版生效。校验其余环境配置未改变，当前留在九周年版。
- 完整改版前源码、配置、数据库与媒体备份在 [`backups/pre-anniversary-9-20260922T182443+0800/`](../../../backups/pre-anniversary-9-20260922T182443+0800/)。归档解包哈希与数据库目录读取已验证；本轮未执行数据库恢复演练。
- 回退外观优先使用[版本切换](../../anniversary-editions.md)。[素材与提示词记录](../../assets.md)说明了三件生成装饰与原生 SVG 的边界。

本轮更新了本地源码和运行中的开发预览，完成生产构建；未重新部署 Docker 应用镜像或公网服务器。
