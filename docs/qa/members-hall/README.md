# 主要成员页：名人堂装饰

2026-09-23。主智能体完成设计、截图比较与最终视觉复核。

在既有冷色、科学图版语言中加入页首交叠展框、外围矩形线条、头像细环和淡色成员展签。
文字与图片保持清晰，装饰不接受鼠标事件，也不进入辅助技术的阅读顺序。
`MemberList` 仅在 `/members` 显式开启 `hall` 外观；静态页中的成员模块保留原样。

## 验证

- 1440 / 768 / 390 / 360px：HTTP 200，无横向溢出或浏览器运行时错误。
- 桌面三列、平板两列、手机单列；页面简介与成员资料继续由原 CMS 管理。
- Chromium（1440 / 360px）和 WebKit（390px）：axe WCAG 2 A/AA、2.1 AA 扫描零违规。
- 仅在浏览器的静态副本中注入长姓名、长角色、长简介、链接、头像和空列表，未修改数据库。
  长文本可换行；链接触控高度不低于 44px，键盘焦点轮廓可见；头像无滤镜或透明度改写。
  WebKit 使用其包含链接的 Option-Tab 导航方式。
- 装饰 SVG 为 `aria-hidden`，保持 `main#main` 跳过导航入口。
- `/pages/about` 的嵌入成员模块没有启用名人堂样式。
- 类型检查、ESLint 与生产构建通过。未新增客户端动画或依赖。

`members-*.png` 是实际开发成员页；`empty-360.png` 与 `long-profile-390.png`
为浏览器视觉夹具。页面中开发示例成员的标记保持可见，未将虚构成员资料写入生产环境。
结果明细见 `viewports.json` 与 `accessibility-and-fixtures.json`。
